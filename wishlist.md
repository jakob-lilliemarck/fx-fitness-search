Stuff to track:
- evaluation time
- evaluation resource usage? if at all possible?
- epoch MSE progression?
- evaluation host id??
- batch_size, since it can now differ on different machines
- rename fx_durable_ga.fitness to "evaluations", and store evaluation metadata there..? Also allow to store user-defined data?
- track lineage of genotypes!!!
